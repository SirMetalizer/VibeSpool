name: Build VibeSpool Apps

on:
  release:
    types: [published]
  workflow_dispatch: # Erlaubt den manuellen Start über einen Button!

# WICHTIG: Erlaubt dem Bot, Dateien direkt ins Release hochzuladen
permissions:
  contents: write 

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
    - name: Code auschecken
      uses: actions/checkout@v4

    - name: Python einrichten
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    # NEU: Mac benötigt die ZBar-Systembibliothek für pyzbar!
    - name: Install ZBar (nur macOS)
      if: matrix.os == 'macos-latest'
      run: brew install zbar

    - name: Pakete installieren
      run: |
        python -m pip install --upgrade pip
        # opencv-python und pyzbar für die Webcam hinzugefügt!
        pip install pillow qrcode pyinstaller paho-mqtt opencv-python pyzbar

    - name: EXE bauen (Windows)
      if: matrix.os == 'windows-latest'
      # --hidden-import zwingt den Compiler, die Kamera-Bibliotheken nicht zu vergessen
      run: pyinstaller --noconsole --windowed --onefile --hidden-import cv2 --hidden-import pyzbar --name "VibeSpool_Win" filament_gui.py

    - name: APP bauen und zippen (Mac)
      if: matrix.os == 'macos-latest'
      run: |
        # Auch hier die --hidden-import Befehle hinzugefügt
        pyinstaller --noconsole --windowed --hidden-import cv2 --hidden-import pyzbar --name "VibeSpool_Mac" filament_gui.py
        cd dist
        zip -r VibeSpool_Mac.zip VibeSpool_Mac.app

    - name: Windows EXE als Artifact sichern
      if: matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: VibeSpool-Windows
        path: dist/VibeSpool_Win.exe

    - name: Mac ZIP als Artifact sichern
      if: matrix.os == 'macos-latest'
      uses: actions/upload-artifact@v4
      with:
        name: VibeSpool-Mac
        path: dist/VibeSpool_Mac.zip

    # --- DIE MAGIE: AUTO-UPLOAD INS RELEASE ---
    - name: EXE an Release anhängen (Windows)
      if: github.event_name == 'release' && matrix.os == 'windows-latest'
      uses: softprops/action-gh-release@v2
      with:
        files: dist/VibeSpool_Win.exe

    - name: ZIP an Release anhängen (Mac)
      if: github.event_name == 'release' && matrix.os == 'macos-latest'
      uses: softprops/action-gh-release@v2
      with:
        files: dist/VibeSpool_Mac.zip
